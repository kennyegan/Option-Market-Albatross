name: oma-bot
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy>=1.24.0
  - pandas>=2.0.0
  - scipy>=1.11.0
  - pip:
    # Core dependencies
    - black>=23.0.0          # Code formatting
    - pylint>=2.17.0         # Linting
    - pytest>=7.4.0          # Testing
    - pytest-cov>=4.1.0      # Coverage
    # Optional: QuantConnect Lean CLI (for local backtesting)
    # - lean>=1.0.0
    # Optional: Jupyter for notebooks
    # - jupyter>=1.0.0
    # - ipython>=8.0.0
    # Optional: Visualization
    # - matplotlib>=3.7.0
    # - seaborn>=0.12.0
